EzBz: סטטוס מסך הקבוצות והערות מפגש קבוצתי
תאריך: 19.9.2026. מסמך מסכם של שיחת תכנון והבדיקות שנעשו בעקבותיה.
הוראות ל-Claude Code (לקרוא קודם)
זה מסמך הקשר, לא משימת בנייה. אחרי הקריאה: לאשר בשורה אחת שקראת, ולחכות להוראה.

כללי ברזל לכל הסשן:

קוד: לא לערוך קבצים, לא להריץ מיגרציות, לא לעבור ענף, בלי stash, reset, add או commit, אלא בבקשה מפורשת. העבודה על b7-stage-screen עדיין לא ב-commit.
DB: דרך mcp__supabase__execute_sql מותר רק SELECT. לפני כל הרצה לכתוב את השאילתה המלאה ואת המילים "SELECT בלבד". אם נראה שצריך כתיבה, לעצור ולשאול.
פרטיות: אלה נתוני ילדים. מטא-דאטה ו-count בלבד. לא להריץ שאילתה שמחזירה שמות, טלפונים או תוכן מתאמנים, ולא להעתיק כאלה לקבצים.
מה שלא ניתן לדעת: לכתוב "לא ידוע" ולא לנחש.
בלי em dash בשום פלט, קוד או טקסט אפליקציה.
כל שינוי קוד: ענף נפרד, PR, אישור ויזואלי לפני merge.
סדר עבודה שנקבע
לסגור את B7: בדיקה חיה, commit, push, PR, merge.
PR נפרד: הערות מפגש אישיות למפגש קבוצתי (טבלה חדשה, שינוי קריאה בכמה מסכים).
PR נפרד: מסך הקבוצות.
צמצום הרשאות הרו"ח לפני מדריך אמיתי ראשון ולפני מיגרציה 009 (ר' סעיף RLS).
החלטות
מסומן: [הוחלט] או [הומלץ] כשהמלצה טרם אושרה במפורש.

מבנה המסך:

[הוחלט] נתיבים: /coach/groups, /coach/groups/[id], /coach/groups/[id]/progress, /coach/groups/[id]/progress/[stageNumber]. משקף את המבנה הקיים של trainees. B6, B7, B3, progress_stages לא משתנים. progress_stages נשארת per-trainee, בלי group_id.
[הוחלט] אין מגבלת מספר חברים בקבוצה. חבר שמצטרף מאוחר נכנס למעקב הקבוצתי מהנקודה שבה הצטרף. אין שדה תאריך הצטרפות בסכמה.
[הומלץ] בלי חישוב אוטומטי לאחור לחבר מאוחר. אין הפצה אוטומטית. המדריכה בוחרת ב-sheet, ובדף השלב שורה אחת "הופץ ל-x מתוך n". בלי נקודות נוספות ברשימה, כדי לא להעמיס.
[הומלץ, לא אושר] B3 נשאר מסך מנהלי עם חיפוש חופשי. מסך הקבוצות לא צריך חיפוש בגרסה הראשונה.

הערות מפגש קבוצתי:

[הוחלט] בסוף מפגש קבוצתי תהיה פעולה "עדכן לכולם" שמעבירה את הערת הקבוצה לחברים.
[הוחלט] טבלה חדשה session_member_notes, מפתח ייחודי (session_id, trainee_id). הערת הקבוצה עצמה נשארת בשורת sessions (חלופה א').
[הוחלט] עמודה נפרדת group_note_copy בשורת החבר. "עדכן לכולם" דורס רק אותה, לא את ההערה האישית. B7 מציג שני בלוקים: "מהקבוצה" ואישי. תאריך המפגש ולא מספר מפגש (אין שדה מספר מפגש בקוד).
[הוחלט] sheet עם סימון לכל חבר (ברירת מחדל: כולם מסומנים), לטיפול בנעדר. המצב הרגשי לא מועבר מהקבוצה.
[הומלץ] בלי גישת רו"ח לטבלה החדשה (מידע רגיש על קטינים).

מערך שיעור קבוצתי:

[הומלץ] מערך קבוצתי נשמר ב-stage_lesson_plans עם group_id מלא ו-trainee_id ריק. העתקים לחברים עצמאיים, בלי קישור חי. group_notes לא נכלל ב"עדכן את כולם".
[הומלץ] "עדכן את כולם": sheet עם סימון לכל חבר. מי שהותאם אישית לא מסומן כברירת מחדל. בלי דיאלוג הכל-או-כלום.
[הומלץ] זיהוי "הותאם אישית": עמודת updated_at עם trigger ב-stage_lesson_plans, ועמודת last_distributed_at בשורה הקבוצתית. חבר נחשב מותאם אם updated_at שלו מאוחר מ-last_distributed_at.

תזכורות WhatsApp:

[הוחלט] כלל קיים ב-B5: שליחה ל-phone, ואם ריק נפילה אוטומטית ל-parent_phone. הסכמה ההורית תמיד ל-parent_phone.
[הומלץ, לא אושר] פגישה קבוצתית מתפרקת לחברים, אותו כלל לכל חבר, הודעות נפרדות בלי שמות הילדים האחרים, איחוד כפילויות לפי מספר.
לא קיים היום קוד שליחה (רק reminderTier() ב-calendarMath.js שמחשבת תג UI).
לאמת מול Twilio: דרישת opt-in ותבניות מאושרות, ושאין שליחה לקבוצות WhatsApp. שיקול משפטי לגבי קטינים: לבדוק עם איש מקצוע.
ממצאי בדיקות
מה-DB (נבדק ב-SQL Editor, קריאה בלבד):

stage_lesson_plans: 0 שורות עם group_id, 0 זוגות כפולים. האינדקס הייחודי החלקי מ-009 בטוח כרגע. לחזור על ה-count לפני הרצת 009.
sessions: 3 מפגשים קבוצתיים, 0 עם סיכום. אין הערות קיימות להגר. כנראה נתוני בדיקה.
updated_at: אין ב-public.sessions, public.stage_lesson_plans, public.trainees. יש רק created_at. ה-updated_at שהופיע בשאילתה גנרית שייך ל-auth.sessions.
Triggers: לא נבדק.
CHECK constraints ב-DB החי: לא נבדק.

מקריאת קוד (Claude Code, לא אומת עצמאית, מקור: קובץ:שורה):

009 פנוי. אחרונה קיימת: 008.
stage_lesson_plans (008:6-17): id, coach_id, trainee_id, group_id, stage_number, template_name, session_goal, planned_tools, bring_to_session, created_at. אין CHECK על שילוב trainee_id/group_id.
ספריית תבניות (page.js:75-82) מסננת trainee_id IS NULL AND group_id IS NULL. שורה קבוצתית לא דולפת למאגר. publishToLibrary (actions.js:81-90) מכניס null/null במפורש.
saveLessonPlan (actions.js:38-70): select ואז update/insert, לא upsert עם onConflict. אין שימוש ב-RPC בשום מקום בקוד. דפוס כתיבה מרובה: Promise.all עם ניקוי מפצה ידני (trainees/actions.js:86-89).
markStageFlag (actions.js:7-14) קובע has_plan/has_notes ל-true בלבד ולעולם לא מאפס.
מפגש קבוצתי: שורה אחת ב-sessions עם group_id מלא ו-trainee_id ריק (createSessionWithNotes, actions.js:154-163, ו-CHECK ב-003_add_group_id_to_sessions.sql:11-14). יש שדה summary, emotional_state, next_session_note אחד לכל המפגש. שמירה דרך חבר א' ואז דרך חבר ב' דורסת את אותה שורה.
מומלץ לאמת בבדיקה החיה של B7: לשמור הערה במפגש קבוצתי דרך חבר א', להיכנס דרך חבר ב', ולראות מה מוצג.
trainees.start_date (חודש בלבד) לא נקבע ביצירה. אין מגבלת חברים ב-createGroup, assignTraineeToGroup, createTrainee. בעת יצירת מתאמן נוצרות 12 שורות progress_stages בסטטוס todo.
שינויים מתוכננים
מיגרציות (ריצה ידנית ב-SQL Editor, קובץ לכל יחידה):

009_add_group_notes_to_stage_lesson_plans.sql: עמודת group_notes TEXT, ואינדקס ייחודי חלקי (group_id, stage_number) WHERE group_id IS NOT NULL AND trainee_id IS NULL.
010_add_session_member_notes.sql: טבלה עם id, session_id, trainee_id, coach_id, summary, next_session_note, emotional_state, group_note_copy, created_at. מפתח ייחודי (session_id, trainee_id), ON DELETE CASCADE, RLS למדריכה בלבד לפי coach_id = auth.uid(), בלי מדיניות לרו"ח. לאמת שה-FK ל-coaches(id) תואם למקובל בשאר הקבצים.
011: updated_at עם trigger ב-stage_lesson_plans (ערך התחלתי מ-created_at), ו-last_distributed_at בשורה הקבוצתית.
סדר בטוח: המיגרציה רצה לפני שהקוד התלוי בה מגיע ל-main (הפריסה ב-Vercel אוטומטית מ-main).

מקומות שישתנו ב-PR של הערות אישיות (קריאה, וב-B7 גם כתיבה):

B7: [stageNumber]/page.js (שורות 83-116), actions.js (96-122, 127-172), SessionNotesTab.js.
B6: trainees/[id]/progress/page.js:56-67 (תצוגה מקדימה מתחת לשלב).
קלנדר: calendar/page.js:31,55 (hasSummary), ו-BottomSheet.js:31-33 (סוכם / טרם סוכם בינארי).
פאנל בוקר: morning/page.js (רשימת "חסר סיכום").
"סיכום אחרון" ב-B5 אינו קיים בקוד (פער ידוע ב-CLAUDE.md).
ממצאי ביקורת על התוכנית המקורית (לתקן במימוש מסך הקבוצות)
getMembersWithCustomContent מחזיר כל חבר עם תוכן לא ריק, ולכן אחרי הפצה ראשונה הדיאלוג יקפוץ תמיד. לזהות התאמה לפי updated_at מול last_distributed_at.
בהפצה ראשונה, כש-last_distributed_at ריק: חבר עם מערך אישי לא ריק נחשב "הותאם".
last_distributed_at נקבע אחרי הכתיבה ובתוך אותה פעולה. אם ייקבע לפני, ה-trigger יסמן את כל החברים כמותאמים מיד אחרי ההפצה.
חסימה כשלושת שדות המערך הקבוצתי ריקים (למשל כשנכתב רק group_notes). להחליט במפורש מה לעשות עם שדה ריק חלקי: לדרוס או לדלג. אחרת ההפצה דורסת תוכן חברים בריק ומסמנת has_plan: true.
Promise.all על כתיבות נפרדות אינו אטומי. להחזיר רשימת הצלחה וכישלון לפחות. גם עדכון progress_stages בסוף לא בודק שגיאות. לשקול פונקציית Postgres בטרנזקציה. השרת יאמת מחדש את רשימת המזהים שאושרה.
hasContent בתוכנית סופר גם group_notes. שלב עם הערה בלבד ייראה "מוכן" ויעוות את הפס "X מתוך 12". ההפצה נשענת על שלושת שדות המערך בלבד, ולכן צריך התאמה בין השניים.
לחיצה כפולה מהירה תפיל את האינדקס הייחודי. לטפל בשגיאה בצורה ידידותית.
has_plan לא חוזר ל-false גם כשמנקים את המערך (קיים ב-B7 היום, לא חוסם).
בתוכנית נאמר ש-B7 לא משתנה. בפועל הוא משתנה ב-PR של ההערות האישיות.
הנחיה: להימנע מ-em dash בקידומות טקסט שמוצג באפליקציה.
לתקן במוקאפ מסך מערך השיעור את הטקסט "שמירה תעדכן לכולם" (סותר את כלל העותק העצמאי). ייתכן שכבר תוקן בענף.
RLS של הרו"ח (ממצא)
מדיניות הרו"ח מוגבלת ל-coach_id של המדריכים המשויכים אליה, לפי האימייל ב-JWT. אין דליפה בין רו"חים. אבל:

ALL על: accountants, coaches, coordinators, documents, groups, income, institutions, payments, progress_stages, sessions, trainees. with_check הוא null בכולן.
SELECT בלבד על: stage_lesson_plans, trainee_payments.
זה סותר את העיקרון הנעול (SELECT בלבד על נתוני מדריך).
מור בחרה במכוון להשאיר גישה מלאה בזמן הפיתוח, כי היא המשתמשת היחידה. ה-SQL Editor ו-service role עוקפים RLS ממילא.
מתוך האפליקציה כרו"ח: stage_lesson_plans ו-trainee_payments לקריאה בלבד.

שורה להוספה ל-CLAUDE.md (שינוי תיעוד בלבד):

RLS של הרו"ח: זמני ורחב בכוונה בזמן הפיתוח. ALL על רוב הטבלאות (כולל sessions, trainees, groups, progress_stages), ו-SELECT בלבד על stage_lesson_plans ו-trainee_payments. לפני מדריך אמיתי ראשון, ולפני מיגרציה 009, חובה להחליף ל-SELECT ולצמצם ל"כספים בלבד". אין לבנות קוד שכותב לטבלאות אלה בשם הרו"ח.

הצעה לצמצום (טרם הוחלט): להסיר מהרו"ח את sessions, progress_stages, stage_lesson_plans. להחליט בנפרד על trainees ו-groups (RLS עובד לפי שורה ולא לפי עמודה). טבלאות הכספים נשארות, כשהחלטה מפורשת אם הן ALL או SELECT.

שורה נוספת ל-CLAUDE.md (כלל DB):

דרך ה-MCP של Supabase מותר להריץ SELECT בלבד. כתיבה ל-DB רק ידנית על ידי מור ב-SQL Editor.
זרימת ניווט (לפי התוכנית, טרם נבנה)
הכוורת, תא "קבוצות", מוביל ל-/coach/groups.
/coach/groups: כרטיס לכל קבוצה (שם, מועד קבוע, מספר חברים, מפגש קרוב), ומתחתיו מתאמנים ללא קבוצה. מתאמן ללא קבוצה מוביל ישר ל-B6 שלו, בלי מסך ביניים.
/coach/groups/[id]: כפתור הכותרת (שם הקבוצה) מוביל למעקב הקבוצתי. שורת כל חבר מובילה ל-B6 האישי שלו (/coach/trainees/[id]/progress).
/coach/groups/[id]/progress: 12 שלבים, נקודת "יש תוכן קבוצתי" בלי סטטוס. לחיצה על שלב מובילה ל-/coach/groups/[id]/progress/[stageNumber]. אין קישור ישיר למתאמנים ממסך זה.
חיפוש: לא במסך הקבוצות בגרסה הראשונה [הומלץ, לא אושר]. חיפוש חופשי נשאר ב-B3.
[הומלץ, ממתין לאישור] נשארים שני מסכים נפרדים (פרטי קבוצה ומעקב קבוצתי), בלי איחוד: אורך רשימת החברים אינו מוגבל, ולכל מסך תפקיד שונה.
שאלות פתוחות
איפה כותבים את הערת הקבוצה ולוחצים "עדכן לכולם": במסך המפגש בקלנדר או במסך הקבוצה? היום מפגש קבוצתי נכנס רק דרך בורר חברים.
אישור סופי להצעת התזכורות הקבוצתיות (ר' סעיף WhatsApp).
החלטה על גישת הרו"ח ל-trainees ו-groups, והיקף מוחלט ל"כספים בלבד".
בדיקה חיה של B7 (כולל תרחיש הדריסה בין חבר א' לחבר ב').
תיקון חיבור ה-MCP של Supabase: טוקן חדש, קריאה בלבד ומוגבל לפרויקט.
Triggers ו-CHECK constraints ב-DB החי: לא נבדקו.
כמה קבוצות יש למדריכה טיפוסית? לא ידוע. להבהיר מול 2-3 מדריכי NLP אמיתיים. קבוצה היא עד 3 נערים לפי האפיון, אך בפועל אין מגבלה.

## החלטות מעודכנות אחרי בדיקה חיה של B7 (21.9.2026)
סעיף זה גובר על סעיפים קודמים בקובץ בכל מקום שבו יש סתירה.

תוצאות הבדיקה החיה (מדריך הבדיקה test-coach-b7):
- מערך שיעור: שמירה ורענון עברו. בחירה מהמאגר, שמירה ושליחה למאגר עברו. העתק עצמאי, המאגר לא משתנה.
- הערות מפגש: מפגש עבר יחיד בלי סיכום מתמלא אוטומטית. שני המצבים האחרים (כמה מפגשים, אין מפגש) לא נבדקו.
- ממצא מאושר: הערות במפגש קבוצתי נשמרות פעם אחת ב-sessions ומוצגות לכל חברי הקבוצה. חבר ב' רואה את מה שנכתב דרך חבר א'. מערך שיעור לא מופץ בין חברים.
- תקלות: הקישור "צפה במערך המלא" בלשונית הערות מפגש לא מגיב. הכותרת "הוסף סיכום" ואחריה "לקבוצה" מכילה מקף ארוך ביניהן.

מודל מעודכן:
1. מערך שיעור הוא קבוצתי ומופץ: נכתב בשלב קבוצתי ומועתק כעותק עצמאי לכל חבר, בלי קישור חי. כפתור "עדכן את כולם" בדף השלב הבודד, מתחת לשדות. sheet עם סימון לכל חבר, ברירת מחדל מסומנים, ומי שהותאם אישית לא מסומן. מותר להתאים מערך לחבר.
2. הערות מפגש אישיות לגמרי ולא מועתקות (סיכום, מצב רגשי). במפגש קבוצתי נשמרות ב-session_member_notes, שורה לכל חבר.
3. זוג שדות חדשים במערך השיעור: "מה היה במפגש הקודם" ו"מה לבדוק במפגש הבא". הם תמיד מופיעים יחד בכל מקום: לשונית מערך שיעור, באנר "ממה המשכנו", דף השלב הקבוצתי, וה-sheet של ההפצה. אין מקום שמציג אחד בלי השני. שתי עמודות חדשות ב-stage_lesson_plans, והם מופצים לחברים.
4. העמודה next_session_note ב-sessions מוסתרת מטופס הערות המפגש, לא נמחקת ובלי מיגרציה הרסנית.
5. [הומלץ, ממתין לאישור] הבאנר ב-B7 מציג את הזוג העדכני ביותר של אותו מתאמן מכל שלב (לפי updated_at), ולא רק מהשלב הנוכחי.
6. group_notes ו-group_note_copy יוצאים מהתכנון. נשארים updated_at עם trigger ו-last_distributed_at לזיהוי "הותאם אישית".
7. מסך אחד, /coach/groups: שדה חיפוש בראש (לפי שם ילד או שם קבוצה; קבוצה שתאמה מוצגת ככרטיס, ילד שתאם כשורה עם שם הקבוצה בתת-כותרת ולחיצה עליו מובילה ל-B6 שלו). כרטיסי קבוצות ממוינים לפי המפגש הקרוב (מפגש היום נחשב קרוב, קבוצה בלי מפגש עתידי בסוף לפי סדר אלפביתי). מתחתיהם מתאמנים בלי קבוצה. בתחתית "הוסף קבוצה" ו"הוסף מתאמן". מחליף את ההפניה הזמנית ל-/coach/trainees?view=groups.
8. סדר PRים: (א) סגירת B7: בדיקה חיה עברה, ממתינים לתיקונים ול-commit. (ב) PR מסך הקבוצות, כולל מיגרציות (אינדקס ייחודי חלקי, שני שדות הזוג, updated_at עם trigger, last_distributed_at) והעברת השדות. (ג) PR הערות אישיות: session_member_notes ושינוי קריאה ב-B6, B7, קלנדר ופאנל בוקר. מספור המיגרציות ייקבע בעת התכנון.
9. פתוח: כמה קבוצות למדריכה טיפוסית (לא ידוע), תזכורות WhatsApp קבוצתיות, וצמצום הרשאות הרו"ח לפני מדריך אמיתי ראשון.

## עדכון (21.9.2026, אחרי מיזוג B7)
סעיף זה גובר על כל סעיף קודם בקובץ בכל מקום שבו יש סתירה.
מצב: B7 מוזג ל-main כ-PR #9 (squash 040ffdd). תיעוד ב-CLAUDE.md נדחף (33027d4). הענף b7-stage-screen לא נמחק.
בוטל: כלל "זוג שדות" ושתי עמודות חדשות; group_notes, group_note_copy ושדה נפרד "מה היה במפגש הקודם". הקישור "צפה במערך המלא" הוסר מ-B7.
מודל:
1. תא "קבוצות" בכוורת מוביל ל-/coach/groups: חיפוש בראש לפי שם ילד או שם קבוצה, כרטיסי קבוצות ממוינים לפי המפגש הקרוב (מפגש היום נחשב קרוב, קבוצה בלי מפגש עתידי בסוף), מתחתיהם מתאמנים בלי קבוצה, ובתחתית "הוסף קבוצה". תוצאת חיפוש: קבוצה שתאמה ככרטיס, וילד שתאם כשורה עם שם הקבוצה, ולחיצה עליו מובילה ל-B6 שלו.
2. לחיצה על קבוצה מובילה ישר ל-12 השלבים שלה, בלי מסך פרטי קבוצה (הוחלט). [הומלץ, ממתין לאישור] שורה קטנה בראש עם שמות החברים, בלי קישורים. רשימת 12 השלבים נקייה: שמות שלבים והנקודה "יש תוכן קבוצתי", בלי באנר ובלי טקסט מהמפגש הקודם.
3. מסך שלב קבוצתי: למעלה באנר "ממה המשכנו". באמצע שדות המערך (מטרה, כלים ופעילויות, להביא) עם "בחר מהמאגר" ו"שלח למאגר". מתחת שדה אחד: "נקודות להמשך למפגש הבא" (עבר מהערות מפגש, שם מוסתר ולא נמחק). בתחתית "עדכן את כולם".
4. "עדכן את כולם": sheet עם סימון לכל חבר, כולם מסומנים כברירת מחדל, ומי שהותאם אישית לא מסומן [הומלץ]. מועתקים כל השדות כולל "נקודות להמשך", כעותק עצמאי. אחרי ההעתקה: קישור לכל חבר ל-B7 שלו להערות האישיות.
5. הערות מפגש אישיות לגמרי ולא מועתקות. במפגש קבוצתי נשמרות ב-session_member_notes, שורה לכל חבר.
6. כלל הבאנר: הבאנר אינו מתעדכן בשמירה. הוא מציג את "נקודות להמשך" האחרונות שנשמרו לפני המפגש האחרון שהתקיים (תאריך המפגש כבר עבר). כל עוד המפגש הבא לא התקיים, הבאנר לא משתנה. לכן "נקודות להמשך" נשמר עם היסטוריית גרסאות עם זמן, ולא כשדה שנדרס. אופן האחסון (טבלת גרסאות או קישור למפגש) ייקבע בתכנון. אין באנר אם אין נקודות שנשמרו. הבאנר קורא מהנקודות העדכניות של הקבוצה מכל השלבים. העמודה handoff_updated_at בוטלה.
7. נשארים: updated_at עם trigger ב-stage_lesson_plans, last_distributed_at בשורה הקבוצתית, אינדקס ייחודי חלקי (group_id, stage_number).
שינויים נוספים: (א) קלנדר, ל-PR ההערות האישיות: בחלונית של מפגש קבוצתי במקום "+ סיכום מפגש" שמות החברים עם סטטוס סיכום לכל חבר, ולחיצה על שם פותחת את B7 שלו; מסך בחירת החבר מבוטל; גם במפגש אישי שורה אחת עם שם המתאמן. (ב) כרטיס מתאמן, PR נפרד: שלבי הציר לחיצים ומובילים ישר ל-B7, גלילה בין כל 12 השלבים, וחץ החזרה חוזר למקור. B6 נשאר. (ג) ניקוי תיעוד: מקפים ארוכים במחרוזות של morning, וקלאס .sd-plan-preview-link שאינו בשימוש.
סדר PRים: (א) רשימת קבוצות והרשימה של 12 השלבים, (ב) מסך שלב קבוצתי והפצה, (ג) הערות אישיות וחלונית הקלנדר, (ד) קיצור הכרטיס. מספור מיגרציות ייקבע בתכנון.

## עדכון (23.9.2026, אחרי מיזוג PR א')
סעיף זה גובר על כל סעיף קודם בקובץ בכל מקום שבו יש סתירה.
מצב: PR א' (רשימת קבוצות ו-12 שלבים לא לחיצים) מוזג ל-main כ-PR #10 (squash 924b5ad). כולל חילוץ CreateGroupSheet ו-CreateTraineeSheet מ-TraineesList, ו-revalidatePath('/coach/groups') בארבע פעולות ב-trainees/actions.js. אין מיגרציה בו. הענף groups-list לא נמחק.
פערים שנתגלו בבדיקה החיה, לרישום בלבד, לא תוקנו:
(א) קביעת מפגש עתידי מהקלנדר לא עבדה בכלל, ומכרטיס מתאמן המפגש נשמר אך לא השתקף בתצוגות. לא קשור ל-PR #10, דורש חקירה נפרדת.
(ב) מסך 12 השלבים של קבוצה נראה עמוס. לשקול קיפול או עיצוב מחדש ב-PR הבא, אחרי שתוגדר משמעות "שלב נוכחי" ברמת קבוצה.
